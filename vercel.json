{
    "builds": [{
      "src": "solutionsexperts/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "15mb", "runtime": "python3.13.15" }
    }],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "solutionsexperts/wsgi.py"
      }
    ]
}